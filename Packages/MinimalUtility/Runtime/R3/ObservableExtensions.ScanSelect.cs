using System;
#if ENABLE_R3
using R3;
#endif

namespace MinimalUtility.R3
{
    public static partial class ObservableExtensions
    {
        // public static Observable<TResult> ScanSelect<TSource, TResult>(this Observable<TSource> source, Func<TSource, TResult> selector)
        // {
        //     return new ScanSelect<TSource, TResult>(source, selector);
        // }
    }

    // internal sealed class ScanSelect<>
}